export default class Ghost{
    constructor(width, height, x, y, color){
        this.width = width;
        this.height = height;
        this.x = x;
        this.y = y;
    }
    colors = ['#1a8cff', '#99ff66', '#ff5050', '#cc66ff'];
}